services:
  nekro-user-panel:
    build: .
    container_name: nekro_user_panel
    restart: always
    network_mode: host
    volumes:
      - ./instances.json:/app/instances.json
    environment:
      - PANEL_HOST=0.0.0.0
      - PANEL_PORT=9054
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=Wdx030014
      - PANEL_JWT_SECRET=nekro-user-panel-secret-change-me
      - PANEL_JWT_EXPIRE_HOURS=24
